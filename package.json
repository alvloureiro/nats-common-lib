{
  "name": "nats-common-lib",
  "version": "1.0.0",
  "description": "Base Types to use with Nats Streaming Library",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del ./build/*",
    "build": "yarn clean && tsc"
  },
  "keywords": [],
  "author": "André Loureiro",
  "license": "MIT",
  "dependencies": {
    "node-nats-streaming": "^0.3.2"
  },
  "devDependencies": {
    "del-cli": "^3.0.1",
    "typescript": "^4.0.2"
  }
}
